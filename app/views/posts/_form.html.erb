<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :grass_tags %>
    <%= form.text_field :grass_tags %>
  </div>

  <div class="field">
    <%= form.label :only_followers %>
    <%= form.check_box :only_followers, {}, true, false %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
